# Copy to .env.local (same folder as package.json). DO NOT commit .env.local.
# BE: chạy profile "http" hoặc "https"/"https-tls" — luôn có HTTP API tại cổng 8080.
# Swagger HTTPS có thể là https://localhost:7296; FE chỉ gọi http://localhost:8080.
NEXT_PUBLIC_API_URL=http://localhost:8080
